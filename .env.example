# HuggingFace
HF_API_TOKEN=
HF_MODEL_ID=bayhaqieee/qwen3.5-9b-nlaw-gguf

# Milvus
MILVUS_HOST=localhost
MILVUS_PORT=19530
COLLECTION_NAME=nusantara_law
EMBEDDING_MODEL=qwen3-embedding:8b

# SearXNG
SEARXNG_URL=http://localhost:8080
SEARXNG_THRESHOLD=0.70

# RAG Config
TOP_K_CHUNKS=5

# LLM Hyperparameters — Chatbot (production, fixed)
MAX_NEW_TOKENS=512
TEMPERATURE=0.1
TOP_P=0.9
TOP_K=40
REPEAT_PENALTY=1.1

# ── Evaluation Hyperparameter Scenarios ──────────────────────────
# These ONLY affect the Fine-Tuned model during evaluation runs.
# Select a scenario from the evaluation dashboard dropdown.
# See Idea/rag_hyperparameter.md for full rationale.
#
# EXP-01 Conservative: temp=0.05, top_p=0.85, top_k=20, rep=1.3
# EXP-02 Balanced:     temp=0.10, top_p=0.90, top_k=40, rep=1.55
# EXP-03 Explorative:  temp=0.25, top_p=0.92, top_k=50, rep=1.4
# EXP-04 Creative:     temp=0.40, top_p=0.95, top_k=60, rep=1.2