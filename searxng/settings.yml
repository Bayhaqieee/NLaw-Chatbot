use_default_settings: true
server:
  secret_key: "nusantaralaw_secure_secret"
  limiter: false
  image_proxy: false
  http_protocol_version: "1.0"

search:
  safe_search: 0
  default_lang: "id"
  formats:
    - html
    - json

engines:
  - name: google
    engine: google
    language: id
    shortcut: g
